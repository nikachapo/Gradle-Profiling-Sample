# <root-project>/scenarios.txt
standard_build {
    tasks = [":app:assembleDebug"]
    git-checkout = {
        build = "master"
    }
    cleanup-tasks = ["clean"]
    warm-ups = 2
}

enable_java_fork_compiler {
    tasks = [":app:assembleDebug"]
    git-checkout = {
        build = "scenarios/6-fork-java-compiler"
    }
    cleanup-tasks = ["clean"]
    warm-ups = 2
}
