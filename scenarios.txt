# <root-project>/scenarios.txt
kapt {
    tasks = [":app:assembleDebug"]
    git-revert = ["b1b1c7c1"]
    cleanup-tasks = ["clean"]
    warm-ups = 2
}

ksp {
    tasks = [":app:assembleDebug"]
    cleanup-tasks = ["clean"]
    warm-ups = 2
}